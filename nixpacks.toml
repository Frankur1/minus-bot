[phases.setup]
nixPkgs = ["python311", "ffmpeg", "git", "wget"]

[phases.install]
cmds = [
  "python -m pip install --upgrade pip setuptools wheel",
  "python -m pip install -r requirements.txt"
]

[start]
cmd = "python bot_minus.py"
